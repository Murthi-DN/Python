#A collection which is Un-Changeable or Immutable

#Tuple is faster than list, since we don't change values, it's faster

tup=(1,2,3,4,5)

--------------------------------------------------------------------------------------------

print(tup[1])

print(tup.index(2))
print(tup.count(5))

--------------------------------------------------------------------------------------------

print(max(tup))
print(min(tup))
print(sum(tup))

--------------------------------------------------------------------------------------------