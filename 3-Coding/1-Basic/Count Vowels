vowels = 'aeiou'

str = 'Hi Murthi'

# make caseless comparisions
str = str.casefold()

# make a dictionary with each vowel a key and value 0
count = {}.fromkeys(vowels,0)

for char in str:
   if char in count:
       count[char] += 1

print(count)