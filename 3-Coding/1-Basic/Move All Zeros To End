Move all zeros to end

List:

a = [1, 2, 0, 0, 0, 3, 6]
a.sort(reverse=True)

------------------------------------------------

Array

arr = [18, 0, 4, 0, 0, 6]
count = 0
for i in range(len(arr):
    if arr[i] != 0:
        arr[i], arr[count] = arr[count], arr[i]
        count += 1